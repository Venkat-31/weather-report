import React from 'react';
import { AppTitle, HeaderContainer } from './styed';

const Header: React.FC = () => {

    return (
        <HeaderContainer>
            <AppTitle>Weather Report</AppTitle>
        </HeaderContainer>
    );
};

export default Header;
